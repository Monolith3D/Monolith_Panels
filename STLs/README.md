# STLs

Size-specific horizontal and vertical parts are grouped into subfolders, while shared clips, corners, clamps, and handle parts are kept here at the root.

Use the matching [BOM](../BOM/) to check which files and quantities are needed for a specific panel set.
